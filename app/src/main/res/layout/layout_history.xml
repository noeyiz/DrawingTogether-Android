<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="data"
            type="com.hansung.drawingtogether.view.history.HistoryData" />

        <variable
            name="vm"
            type="com.hansung.drawingtogether.view.history.HistoryViewModel" />
    </data>
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="12"
            android:padding="10dp"
            android:orientation="vertical"
            android:background="@android:color/holo_orange_light">

            <ImageView
                android:id="@+id/history_thumbnail"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="6"
                android:scaleType="fitXY"
                android:adjustViewBounds="true"
                app:imageSrc="@{data.image}"/>

            <ImageView
                android:id="@+id/history_more"
                android:layout_width="25dp"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:scaleType="fitXY"
                android:layout_gravity="right"
                android:adjustViewBounds="true"
                android:src="@drawable/more"/>

        </LinearLayout>

        <TextView
            android:id="@+id/history_topic"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{data.topic}" />

        <TextView
            android:id="@+id/history_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{data.time}"/>

    </LinearLayout>
</layout>
