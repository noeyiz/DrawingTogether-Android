package com.hansung.drawingtogether.view.drawing;

public enum Mode {
    DRAW, ERASE, SELECT, GROUP, TEXT, CLEAR, CLEAR_BACKGROUND_IMAGE, UNDO, REDO,
    BACKGROUND_IMAGE,
    MID,
    WARP,
    AUTODRAW;
}
